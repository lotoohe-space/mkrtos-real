cmake_minimum_required(VERSION 3.13)

# set(deps)
# file(GLOB_RECURSE deps *.S)

# # message("deps = ${deps}")

# add_library(dietlibc_syscall_s OBJECT ${deps})

# target_include_directories(
#     dietlibc_syscall_s
#     PUBLIC
#     include_directories(${CMAKE_CURRENT_LIST_DIR}/)
# )
